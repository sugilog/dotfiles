name: journal
root: ~/
windows:
  - main:
      layout: even-horizontal
      panes:
        - TERM=screen-256color irssi
        - cd $HOME/Dropbox/Journal && vim `date +%Y%m.md` `date +strategy.%Y%m.tracwiki` -p
